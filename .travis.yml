sudo: required
services: 
    - docker
before_install:
    - docker build -t grhovo/docker-react -f Dockerfile.dev .
script:
    - docker run -e CI=true grhovo/docker-react npm run test -- --coverage 